# Configs
This folder stores reusable configuration examples for training and inference.
Current files:
- default_training_config.json
- default_inference_config.json
These JSON files are documentation-friendly defaults and can be mirrored in future CLI or notebook workflows.